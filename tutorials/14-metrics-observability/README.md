# Tutorial 14: Prometheus Metrics & Observability

Expose Prometheus-compatible operational metrics for CPU, memory, and HTTP request throughput.

## Key Concepts
- Exposing text-formatted metrics at `/metrics`.

## Build and Run

```bash
mkdir build && cd build
cmake ..
make
./tut14
```
