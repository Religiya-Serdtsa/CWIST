# Tutorial 12: Cookie & Session Management

Issue secure HTTP-only cookies and manage server-side user session lifecycles.

## Key Concepts
- Setting `Set-Cookie` headers for session persistence.

## Build and Run

```bash
mkdir build && cd build
cmake ..
make
./tut12
```
