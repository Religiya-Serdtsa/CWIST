# Tutorial 16: Programmatic HTML Document Builder

Build HTML element trees programmatically in C using the CWIST HTML builder API.

## Key Concepts
- Node-based HTML creation with `cwist_html_element_create` and `cwist_html_render`.

## Build and Run

```bash
mkdir build && cd build
cmake ..
make
./tut16
```
