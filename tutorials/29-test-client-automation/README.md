# Tutorial 29: Automated In-Memory Integration Testing

Write fast, zero-socket in-memory API integration tests using `cwist_test_client`.

## Key Concepts
- Dispatching synthetic HTTP requests in unit tests with `cwist_test_client`.

## Build and Run

```bash
mkdir build && cd build
cmake ..
make
./tut29
```
