# Tutorial 26: Generic ORM Query Builder

Construct type-safe database queries in C11 using macro-based generic dispatch (`_Generic`).

## Key Concepts
- Type-safe ORM query generation and object mapping in C.

## Build and Run

```bash
mkdir build && cd build
cmake ..
make
./tut26
```
